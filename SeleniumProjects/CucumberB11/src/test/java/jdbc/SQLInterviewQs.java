package jdbc;

public class SQLInterviewQs {
    /*
    1.What is SQL?
    Structured query language to access and create database.

    2.Which clauses fo you mostly use when writing a query?
    sql clauses used for limiting queried responses.
    where -> we define the condition and check if it is true it can be used
             with single row function but can't be used with multi-row function
    group by -> it is used after the where clause   it divides data in to groups
    having   -> used to search for condition after group by
                can be used for multi-raw function
                Having can be used in the absence of a group.
    order by -> using single or multiple column we could arrange the data in ascending or descending order





     */
}
